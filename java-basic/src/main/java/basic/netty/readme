
1.客户端提交任务给服务器端
2.服务器端获取到任务后，异步的执行
3.客户端可以异步的提交任务也可以同步的提交任务
4.客户端处理并发的请求，也是多个io线程来处理的



通过上面的介绍可以抽象出一下几个类
server：服务器端的核心类，负责服务器端的开始、结束、初始化操作
threadpool:核心线程类操作，负责管理维护io的线程操作
client：客户端的操作类信息
invokeFuture：结果的操作信息
